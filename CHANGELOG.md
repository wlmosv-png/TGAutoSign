# 更新日志

## v2.0.0（2026-09-08）—— 加上界面，能看也能改

这版最大的变化：模块从"装完就隐身"变成"有图标、有界面、能折腾"。

**界面部分**

- 桌面上有了 App 图标，打开就是主页：
  - 最上面一张状态卡，显示模块到底有没有注入进 Telegram、今天已经签了几个目标——以前全靠猜，现在一眼就知道
  - 中间是目标列表，每个机器人显示 uid、签到指令、今日状态（已签 / 待签 / 重试中 / 退避中 / 已放弃）
  - 点目标可以改指令、重置当天状态、删除、或者手动触发一次签到
  - 右下角 ➕ 手动添加：填机器人 ID 和签到指令，确定后立即出现在列表里，同时通知模块立刻签一次
- 日志页：模块的运行日志实时滚动显示，不用再翻系统 logcat，插件出问题自己就能看
- 设置页：
  - 学习关键词（网络层自动识别签到语时按这些词过滤）
  - 每日重试上限
  - 签到结果通知开关
- 签到结果从 Toast 升级成系统通知（Android 13 以上需要在系统设置里给 App 开通知权限，第一次装会提示）
- 桌面小组件：显示"今日已签 x / n"，点击直接进主页

**这版踩的坑，写下来免得后人再跳**

1. 最初以为 App 和模块共享数据很简单——App 写文件、模块读文件不就行了？实际上模块是被注入到 Telegram 的进程里去跑的，代码里的 context 是 Telegram 的，写文件都写进了 Telegram 的数据目录，App 根本看不到。后来换成外部存储目录也一样，跨包根本不通。
2. 换 ContentProvider 跨进程读写，结果系统权限把这路也卡死了，App 和模块照样对不上。
3. 最后改成广播桥：模块在 Telegram 进程里发心跳广播，App 收到就知道"模块活着"；App 发状态请求，模块把配置、今日状态、最近 200 行日志打包回传；App 的操作也走广播命令过去。广播跨进程没有文件权限问题，这套就稳了。

**其他**

- 旧版（v1.x）学过的目标数据会自动迁移过来，不用重新点一遍
- 配置从原来的散健整合成 config / state 两份 JSON，好维护
- 模块和 App 之间设了心跳，App 能提示"模块未注入"并给出排查步骤

## v1.1.0（2026-09-07）—— 稳定性与成功率

- 回调型按钮识别：有些机器人的签到按钮走 callback 不走发消息，以前这类会静默漏签，现在识别出来并提示
- Bot 回复语义判定：发出签到后，如果机器人回复里带"失败 / 请先 / 无法 / 已过期 / 没有资格"这类词，自动撤销"已签"标记、安排退避重试，并提示你查一下
- 错误分类：
  - 永久性错误（机器人不存在 / 被拉黑 / 无权发送等）当天直接放弃，不再反复骚扰
  - 频率限制（420 / FLOOD_WAIT）等 60 秒自动重试一次，不占重试次数
  - 一般错误按指数退避重试：5 分钟 → 15 分钟 → 45 分钟 → 2 小时 → 4 小时
- 多账号感知：切换 Telegram 账号后自动重载对应账号的目标，不会拿 A 账号的目标去 B 账号发
- 启动补签从 5 秒推迟到 10 秒，等网络/登录就绪再动手，减少首发失败
- 修复一个日志 ×2 的问题：sendRequest 有多个重载，同一条消息会被 hook 两次，做了 500ms 窗口去重
- 学习关键词补上 /qd、/qiandao、/sign，之前这些常见签到指令会被过滤器误拦

## v1.0.0（2026-09-07）—— 第一个可用版本

- 自动学习：手动点一次机器人的签到按钮，自动记住机器人和签到指令；网络层也能识别（只看机器人的短指令，带关键词过滤）
- 每天只签一次，发了就算，当天不再重复
- 全部签完会提示"今天已经签到过了"
- 断网自动跳过，网络恢复补签
- 打开聊天、Telegram 有任何网络活动、每 30 分钟兜底，都会补查一遍
