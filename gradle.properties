org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# 默认让 AGP 自己从 google() 仓库取 aapt2（普通 PC 直接可构建）。
# 只有编译机上确实有这个二进制时才需要覆盖，也可以命令行临时加：
#   ./gradlew -Pandroid.aapt2FromMavenOverride=/opt/android-sdk/aapt2bin/aapt2 :app:assembleRelease
# android.aapt2FromMavenOverride=/opt/android-sdk/aapt2bin/aapt2
