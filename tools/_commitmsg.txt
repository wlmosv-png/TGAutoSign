feat(v1.5.7/120): 界面国际化（英文开箱即用）+ 多账号数据隔离 + 构建期双重门禁

界面国际化
- 新增 Lang.java：tr/tf/trShort 三个 API，语言模式 0=跟随系统 1=中文 2=英文，
  设置页「界面语言」三态开关，手动切换才持久化 jmb_lang
- 覆盖主菜单、设置、目标列表、补签列表、教程、日志页按钮、分享与对话框，
  以及发到收藏夹的每日摘要与连续失败告警
- 英文下徽章与短按钮用短词表（Sign/Targets/Wake…）并允许折行，
  中文布局零改动，英文才放宽/收紧边距

多账号
- 待签记录按账号前缀区分：两账号共用同一 bot 时不再互相顶掉
- 新增「账号一览」界面，可逐账号查看与启停
- 配置按账号隔离，新增 applyConfigToAllAccounts / migrateAccountConfigs

修复
- 定时模式「约 X 分钟后」为拼接字符串，外层 Lang.tf 挡不住内层中文 → 改整句模板
- 「出品」词条曾译成空串，英文只剩 (c) wlmosv → 改整句模板
- 诊断页宿主包/当前账号/目标数/标志类四项直接拼接输出 → 全走模板
- 删除确认框按钮、系统分享面板标题、日志页筛选与空态未译
- 源码树 module.prop 落后于实际发布版本（1.5.5/118 vs 线上 1.5.6/119）→ 补齐

构建期门禁
- build.sh 挂 i18n 检查：UI 文案漏翻译直接构建失败（6 类判定 + 6/6 回归自测）
- build.sh 挂版本四查：build.gradle / UpdateChecker / module.prop / CHANGELOG 必须同版本
- i18n/ 工具链：check_i18n.py、sync_dict.py、en.tsv(539)、en_short.tsv(18)、selftest.sh
